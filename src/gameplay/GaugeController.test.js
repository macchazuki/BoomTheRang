import { describe, it } from 'vitest';

describe('GaugeController contract', () => {
  it.todo('reflects normalized marker at 0 and 1');
  it.todo('classifies red/green/white using configured widths');
  it.todo('uses deterministic exact-boundary behavior');
  it.todo('keeps upgraded widths valid and totaling 100%');
});
